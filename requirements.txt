# Python-Abhängigkeiten für die IFJN Publishing-Pipeline

# Für das einfache Lesen und Schreiben von YAML Frontmatter in Markdown-Dateien
PyYAML>=6.0.1

# Kommentarerhaltendes YAML. Gebraucht von tools/doclayout, wenn ein Layout in
# die _quarto.yml eines Buches eingetragen wird: PyYAML baut die Datei dabei
# aus der Datenstruktur neu auf und verliert jede Notiz darin ("Reihenfolge mit
# dem Lektorat abgestimmt -- nicht umsortieren!").
# Kann-Abhängigkeit: Fehlt sie, wird weiterhin mit PyYAML geschrieben, und der
# Bericht sagt ausdrücklich, dass Kommentare verlorengingen.
ruamel.yaml>=0.18

# Qt-UI (einzige UI nach Tk-Purge)
PySide6>=6.6.0

# Kleine Regression-Suite für Nicht-GUI-Kernlogik
pytest>=8.3.0

# orchestrator.py's live multi-agent console output (from GrammarGraph)
rich>=13.0

# KDP Wrap-Cover-Export (tools/kdp_cover)
Pillow>=10.0.0

# PDF lesen. Harte Abhängigkeit, kein Kann: tools/publisher_compliance/
# validators.py und tools/satzpruefer/extract.py importieren ``fitz`` ungeschützt
# im Modulkopf. Fehlt das Paket, lassen sich vier Testmodule nicht einmal
# einsammeln und der ganze pytest-Lauf bricht ab -- die Plugins "Druck-Freigabe
# prüfen" und "Satzprüfung & Regelkreis" starten dann gar nicht erst.
# Benutzt werden nur fitz.open, fitz.Document und fitz.Page; die Untergrenze ist
# entsprechend konservativ gewählt.
PyMuPDF>=1.23

# Zahlen-Arrays. Bisher nur zufällig da: stylecloud und spacy ziehen numpy als
# eigene Abhängigkeit mit. tools/breathcloud/engine.py, tools/stylecloud/
# mask_image.py und must_word.py importieren es aber selbst und ungeschützt --
# fiele es aus einer der beiden Ketten heraus, brächen sie ohne Vorwarnung.
numpy>=1.26

# Cover-Schlagwortwolken (plugins/stylecloud → tools/stylecloud)
# stylecloud nutzt noch pkg_resources → setuptools <82
stylecloud>=0.5.2
setuptools>=70,<82
# Optional für „Nur Substantive“: zusätzlich Modelle laden
#   python -m spacy download de_core_news_sm
#   python -m spacy download en_core_web_sm
spacy>=3.7,<4
click>=8.0
