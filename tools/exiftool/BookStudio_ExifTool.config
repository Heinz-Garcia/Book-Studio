# Book Studio — User-defined PDF Info tag for Production-UUID.
# Used via: exiftool -config <this_file> -PDF:UUID=... file.pdf
%Image::ExifTool::UserDefined = (
    'Image::ExifTool::PDF::Info' => {
        UUID => { Writable => 'string' },
    },
);
1; # end config file
