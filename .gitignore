# Python Virtual Environments
.venv/
venv/
ENV/
env/

# Secrets / Passwords
.env

# Python Cache / Bytecode
__pycache__/
*.py[cod]
*$py.class
# Coverage-Reports (lokal, nicht versionieren)
.coverage
.coverage.*
htmlcov/
.coverage.xml

# Quarto / Pandoc
/_site/
/site_libs/
/.quarto/
/output/

# Betriebssystem-Dateien
.DS_Store
Thumbs.db
desktop.ini

# VS Code
.vscode/
check_staged.py
studio_config.json
Textdokument (neu).txt
session_state.json

# Sanitizer-Sicherungskopien (lokal neben dem Buchprojekt)
_Sanitizer_Backups_*/
sanitizer_backup_*/

# B5: app_config.json wird versioniert (App-Defaults).
# Wenn ein Maintainer lokale Anpassungen vornehmen will, die nicht
# ins Repo sollen, soll er die Datei einmal umbenennen und in dieser
# Zeile ergänzen.